# タスク一覧: パスワード認証によるサインアップ画面の作成 (#20)

## 環境構築
- [x] `react-router-dom` を追加する（package.json）
- [x] `tailwindcss` + `@tailwindcss/vite` を追加する（package.json）
- [x] `vite.config.ts` に Tailwind CSS プラグインを追加する
- [x] `index.css` を Tailwind CSS の読み込みに置き換える

## ルーティング
- [x] `main.tsx` に BrowserRouter を設定する
- [x] `App.tsx` をルート定義に変更する（Routes, Route）

## APIクライアント
- [x] `lib/api.ts` に型定義（SignupRequest, SignupResponse, ErrorResponse, ValidationErrorResponse）を追加する
- [x] `lib/api.ts` に `signup` 関数を実装する

## サインアップ画面
- [x] `pages/SignupPage.tsx` を作成する
  - メールアドレス入力欄
  - パスワード入力欄
  - サインアップボタン
  - フロントエンドバリデーション（メール形式、パスワード8文字以上）
  - APIエラー表示（409重複、422バリデーション、ネットワークエラー）
  - ローディング状態の制御
  - 成功時のダッシュボードへの遷移

## ダッシュボード画面
- [x] `pages/DashboardPage.tsx` を作成する（プレースホルダー）

## テスト
- [x] テスト環境を構築する（vitest, @testing-library/react, @testing-library/jest-dom, jsdom）
- [x] `lib/api.ts` のテストを作成する
  - 正常なリクエストでSignupResponseが返却されること
  - 409エラー時にエラー情報が取得できること
  - 422エラー時にバリデーションエラー情報が取得できること
  - ネットワークエラー時に適切な例外がスローされること
- [x] `pages/SignupPage.tsx` のテストを作成する
  - メールアドレスとパスワードの入力欄、サインアップボタンが表示されること
  - メールアドレスが空の状態で送信するとバリデーションエラーが表示されること
  - メールアドレスの形式が不正な場合にバリデーションエラーが表示されること
  - パスワードが空の状態で送信するとバリデーションエラーが表示されること
  - パスワードが8文字未満の場合にバリデーションエラーが表示されること
  - バリデーションエラー時にAPIリクエストが送信されないこと
  - 正常な入力でサインアップボタンを押すとAPIリクエストが送信されること
  - API成功時(201)にダッシュボード画面（`/`）に遷移すること
  - API失敗時(409)に重複メールのエラーメッセージが表示されること
  - API失敗時(422)にバリデーションエラーメッセージが表示されること
  - ネットワークエラー時にサーバー接続エラーメッセージが表示されること
  - API呼び出し中はサインアップボタンがローディング状態になること
- [x] `pages/DashboardPage.tsx` のテストを作成する
  - プレースホルダーのダッシュボード画面が表示されること

---

実装が完了したらテストを実行し、全て通ればPullRequestを作成して push してください。
テストが通らない場合、tasks.md に通らなかったテストを報告して作業を停止してください。
